[1] "character"
> typeof(yields_list$DATA)
[1] "character"
> colnames(help)
NULL
> ?colnames
> `colnames<-`(yields_list$DATA,"date")
Error in `colnames<-`(yields_list$DATA, "date") : 
  attempt to set 'colnames' on an object with less than two dimensions
> colnames(yields_list)[1]<-"DATE"
> RM(yields_rowcolumn)
Error in RM(yields_rowcolumn) : could not find function "RM"
> rm(yields_rowcolumn)
> last_days_asDate<-as.Date(last_days_list)
Error in as.Date.default(last_days_list) : 
  do not know how to convert 'last_days_list' to class “Date”
> typeof(last_days_list)
[1] "list"
> View(last_days_list)
> colnames(last_days_list)[1]<-"DATE"
> last_days_df<-as.data.frame(last_days_list)
> last_days_asDate<-as.Date(last_days_df)
Error in as.Date.default(last_days_df) : 
  do not know how to convert 'last_days_df' to class “Date”
> View(as.character(last_days_list))
> last_days_asDate<-as.Date(last_days_df$DATE)
> VIew(last_days_asDate)
Error in VIew(last_days_asDate) : could not find function "VIew"
> last_days_asDate<-as.data.frame(last_days_asDate)
> colnames(last_days_asDate)[1]<-"DATE"
> ?as.Date.character
> yields_list_asDate <- as.Date(yields_list, "%m - %d - %Y")
Error in as.Date.default(yields_list, "%m - %d - %Y") : 
  do not know how to convert 'yields_list' to class “Date”
> yields_list_asDate <- as.Date(yields_list$DATE, "%m - %d - %Y")
> mutate(yields_list[,1],as.Date)
Error in UseMethod("mutate") : 
  no applicable method for 'mutate' applied to an object of class "character"
> mutate(yields_list[,1],as.Date())
Error in UseMethod("mutate") : 
  no applicable method for 'mutate' applied to an object of class "character"
> mutate(yields_list[,1],as.Date(yields_list$DATE, "%d - %m - %Y"))
Error in UseMethod("mutate") : 
  no applicable method for 'mutate' applied to an object of class "character"
> mutate(yields_list$DATE,as.Date(yields_list$DATE, "%d - %m - %Y"))
Error in UseMethod("mutate") : 
  no applicable method for 'mutate' applied to an object of class "character"
> dates_list_asDate<-as.Date(yields_list$DATE, "%d - %m - %Y")
> View(dates_list_asDate)
> dates_list_asDate<-as.data.frame(as.Date(yields_list$DATE, "%d - %m - %Y"))
> colnames(dates_list_asDate)[1]<-"DATE"
> yields_list_asDate<-full_join(dates_list_asDate,yields_decim)
Error in `full_join()`:
! `by` must be supplied when `x` and `y` have no common variables.
ℹ use by = character()` to perform a cross-join.
Run `rlang::last_error()` to see where the error occurred.
> 
> 
> yields_list_asDate<-bind_cols(dates_list_asDate,yields_decim)
> View(yields_list_asDate)
> yields_decim<-yields_list_asDate
> yields_lastday<-inner_join(last_days_asDate,yields_decim,by = "DATE")
